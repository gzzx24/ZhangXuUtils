<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apputils.zhangxu.zhangxuutils">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".widgets.WidgetsActivity" />
        <activity android:name=".toolbar.ToolbarActivity" />
        <activity
            android:name=".toolbar.Activity.ToolbarAndAppCompatActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".materialdesign.DesignActivity"
            android:label="@string/title_activity_design"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".materialdesign.activity.DesignWidgetActivity" />
        <activity android:name=".materialdesign.activity.DesignTabActivity"></activity>
    </application>

</manifest>